name: Build H3MP

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1.3
      
    - name: Setup NuGet
      uses: nuget/setup-nuget@v1
      
    - name: Restore NuGet packages
      run: nuget restore H3MP.sln
      
    - name: Build solution
      run: msbuild H3MP.sln /p:Configuration=Debug /p:Platform="Any CPU" /verbosity:minimal
      
    - name: Package mod
      run: |
        mkdir release
        copy bin\Release\H3MP.dll release\
        if exist H3MP.ab copy H3MP.ab release\
        if exist Config.json copy Config.json release\
        if exist SosigWearableMap.json copy SosigWearableMap.json release\
      shell: cmd
      
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: H3MP-Release
        path: release/
        if-no-files-found: error
        
    - name: Create Release (on tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: release/*
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
